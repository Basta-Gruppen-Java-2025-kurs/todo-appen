
spring:
  config:
    import: optional:file:../../../example.env
  application:
    name: todo-appen

  datasource:
    url: jdbc:mysql://localhost:3306/todo_db?createDatabaseIfNotExist=true
    username: ${TODO_USER}
    password: ${TODO_USER}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080